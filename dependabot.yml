version: 2
# Dependabot이 의존성을 확인할 빈도를 설정합니다.
updates:
  # ==========================================================
  # 1. GitHub Actions 의존성 관리
  # ==========================================================
  - package-ecosystem: "github-actions"
    directory: "/" # .github/workflows 파일을 스캔합니다.
    schedule:
      interval: "monthly" # 월별로 점검하여 너무 잦은 PR 방지
    target-branch: "main"
    # Major 버전 업데이트는 수동으로 처리하도록 제한 (안정성 확보)
    ignore:
      - dependency-name: "*"
        update-types: ["major"]
    
  # ==========================================================
  # 2. JavaScript (npm) 의존성 관리 - 보안 우선순위 강화
  # ==========================================================
  - package-ecosystem: "npm"
    directory: "/" # package.json 스캔
    schedule:
      interval: "daily" 
    target-branch: "main"
    
    # 🚨 취약점 PR에 라벨 자동 추가 및 담당자 지정
    labels:
      - "security"
      - "npm-update"
    assignees:
      - "your-github-username" # ⬅️ 여기에 **본인의 GitHub 사용자명**을 입력하세요.
      
    # ⚙️ 그룹 관리: 마이너/패치 업데이트만 묶어서 하나의 PR로 관리
    groups:
      patch-and-minor-updates:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

  # ==========================================================
  # 3. Python (pip) 의존성 관리
  # ==========================================================
  - package-ecosystem: "pip"
    directory: "/" # requirements.txt 스캔
    schedule:
      interval: "weekly"
    target-branch: "main"
    
    # PR 개수 제한 (한 번에 너무 많은 PR 생성 방지)
    open-pull-requests-limit: 10 

  # ==========================================================
  # 4. Dockerfile 의존성 관리 (선택 사항)
  # ==========================================================
  - package-ecosystem: "docker"
    directory: "/" # Dockerfile이 있는 경로
    schedule:
      interval: "weekly"
